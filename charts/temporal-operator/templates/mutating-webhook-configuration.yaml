{{- /*
Skipped in remote mode: the configuration belongs to the cluster holding the custom resources,
which is the watched one. Either the operator applies it there itself (remote.bootstrap.enabled)
or it is applied out of band, see docs/operations/remote-cluster.md.
*/ -}}
{{- if not .Values.remote.enabled }}
{{- include "temporal-operator.mutatingWebhookConfiguration" . }}
{{- end }}
