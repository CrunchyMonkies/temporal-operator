{{- /*
Skipped in remote mode, for the reason given in mutating-webhook-configuration.yaml.
*/ -}}
{{- if not .Values.remote.enabled }}
{{- include "temporal-operator.validatingWebhookConfiguration" . }}
{{- end }}
